










import { Component, Mixins } from 'vue-property-decorator';
import clickaway from 'vue-clickaway';

@Component
export default class DMenu extends Mixins(clickaway.mixin) {
  public menuClose() {
    this.$emit('menuClose');
  }
}
